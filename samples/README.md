# 遥测样本

`local/` 用于存放本机真实遥测，目录内文件默认不提交。

已收到并只读核验 `LMU_SourseData/` 中的 1 份 Monza GT3 练习遥测和配置，并查看 `RacingLabPicture/` 中的 6 张界面参考。两个目录均整体被 Git 忽略，未移动或修改原材料。

核验结果见 [首份样本核验](../docs/sample-verification.md)。用户已确认 4 个展示圈时正确；游戏版本为 2026-09-16 当日最新版（用户确认，具体版本号未知）。无需补充截图即可启动核心通道读取，具体版本号可后补。

可公开或团队共享的最小样本应先获得授权、去除个人标识，再单独确定存放位置和忽略规则。合成测试数据与真实数据必须清楚区分。
