# 协作说明

当前阶段以规划与真实数据核验为主，不存在可执行的构建或测试命令。

## 工作顺序

1. 先阅读 [V0.1 范围](docs/scope-v0.1.md) 与 [开发路线](docs/roadmap.md)。
2. 为具体需求建立任务，写清输入、输出及验收方法。
3. 使用独立分支完成修改，提交说明描述实际变化。
4. PR 中说明验证结果；尚未执行的检查明确标记。

## 数据与实现

- 真实样本放在 `samples/local/`，不要提交账号、密钥、完整私有遥测或个人路径。
- 可共享测试样本须获得授权并去标识化；合成数据明确标为合成。
- 不从字段名称猜测数据单位或接口含义。
- 缺失值、异常值和真实零值分开处理。
- 先覆盖解析边界和用户可观察行为，再添加必要测试；文档占位无需虚构测试通过记录。
- 实际选定技术栈后再加入依赖清单、锁文件、运行命令与 CI。
- 新增自动分析、实时读取或云端功能前先更新并确认范围。
