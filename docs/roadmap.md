# 开发路线

## P0：仓库与规划（本次范围）

- [x] 明确 V0.1 为文件解析与数据展示。
- [x] 建立模块目录、数据约定及界面规划。
- [x] 建立本地样本忽略规则和协作模板。
- [x] 创建 GitHub 私有仓库并同步初始结构（后续变更同步状态以远端为准）。

## P1：真实数据核验（首份完成，仍有待核实项）

- [x] 取得一份用户授权的 LMU 原生 `.duckdb` 文件及配置。
- [x] 核实实际表结构、核心单位、4 个完整计时区间。
- [x] 清点 56 个连续通道和 42 个事件，核对配置。
- [x] 用 Python + DuckDB 1.5.5 只读检查，确认检查前后源文件哈希一致。
- [x] 整理 6 张参考图，记录选圈、联动曲线和去除 AI 面板的要求。
- [ ] 核实连续通道精确采样相位、7 Hz 数量偏差及官方圈有效性。
- [x] 用户确认 4 个展示圈时正确；游戏版本记录为 2026-09-16 当日最新版（用户确认）。
- [ ] 后续补充具体游戏版本号或 Steam Build ID（不阻碍当前开发）。

证据及限制见 [首份样本核验](sample-verification.md)。核心通道可进入最小读取开发，未核实部分保持明确限制。

完成标准：能解释样本结构，圈时与关键通道数值可人工核对，不靠猜测建立字段映射。

## P2：最小读取与模型（本样本验收完成）

- [x] 只读打开文件并提供结构诊断，检查源文件哈希。
- [x] 建立会话、圈次及核心通道模型，支持完整圈 JSON 导出。
- [x] 区分首尾片段、完整性、未知有效性、缺失值和真实零值。
- [x] 实现核心通道时钟重建、圈内插值及挡位事件保持，并提供时间来源标记。
- [x] 18 项合成边界测试 + 1 项真实样本验收通过。

运行方法和兼容边界见 [读取模块说明](reader-usage.md)。未知格式或不可靠时钟明确拒绝，非核心通道和精确源采样相位仍不在已验证范围内。

## P3：基础展示

- [x] 本地网页采用 FastAPI + JavaScript / Canvas，完成文件导入和圈次选择。
- [x] 展示核心通道，提供同步游标、缩放与平移。
- [x] 完成单位、缺失区间及异常状态的显示。
- [x] 在可信距离可用时提供距离横轴和同会话两圈叠加。
- [x] 本地持久存储和车手 / 练习 / 圈次编号，预留云端存储替换边界。
- [x] 新增 7 项 API / 存储检查，与原 19 项读取检查一起通过。
- [x] 圈速总览：全部圈次 / 分段、平均、理论最快与最优分段来源。
- [x] 原始数据目录：98 项通道 / 事件的数值清点和可视化判断。
- [x] 分段核验、缺失与统计范围等新增 12 项检查通过，总计 38 项。
- [x] 经纬度局部走线对比、双向游标联动和弯道跟随，新增 10 项 GPS / 投影 / API 检查，总计 48 项。

## P4：V0.1 验收与交付（2026-09-17 用户确认完成）

当前成果作为 V0.1 交付，见 [交付记录](release-v0.1.md)。下面原定检查保留其实际状态，未完成项作为后续质量工作，不因版本完成而视为已验证。

- [ ] 用真实授权样本人工核对图表、圈时与原始数据。
- [ ] 核对首尾残圈、空通道、不同采样频率、损坏及占用文件。
- [ ] 实测长会话的加载时间、内存和图表交互表现，记录测试设备及样本规模。
- [ ] 确认导入前后源文件内容未变。
- [ ] 补齐依赖锁定、Windows 启动 / 打包及使用说明。
- [x] 用户确认当前实现为 V0.1 完成版本；保留验证限制，不承诺所有车辆和文件兼容。

## V0.2：赛道参照与练习信息（待开发）

- [ ] 调研可用的赛道原始数据、授权与版本 / 布局匹配。
- [ ] 在走线对比中尝试叠加赛道边界、路肩等已验证的参照数据，验证坐标配准与误差。
- [ ] 核验遥测中的车型、天气、赛道状况、轮胎、燃油及调教相关字段。
- [ ] 支持自动读取和用户选择 / 补录，分别保存数据来源、单位、时间与未知状态。
- [ ] 增加练习信息界面，支持查看影响圈速的外部条件及其圈内 / 圈间变化。
- [ ] 检查历史记录兼容、信息修改后持久化、缺失数据和来源冲突。

详细范围、推进顺序与验收标准见 [V0.2 任务](scope-v0.2.md)。

## 后续候选（未承诺）

云端账号、数据迁移 / 存储、赛道图联动、更多通道、导出、实时共享内存接入。云端已预留接口边界，服务器和同步尚未实现。自动驾驶分析和 AI 不属于当前路线，新增范围需另行确定。
